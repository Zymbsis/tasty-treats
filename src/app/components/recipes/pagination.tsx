import { PaginationMetadata } from "@app/lib/types/api.types";

const Pagination = ({
  // eslint-disable-next-line @typescript-eslint/no-unused-vars
  paginationMetadata,
}: {
  paginationMetadata: PaginationMetadata;
}) => {
  return <div>Pagination</div>;
};

export default Pagination;
