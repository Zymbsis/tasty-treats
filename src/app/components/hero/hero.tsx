import HeroDescription from "@app/components/hero/hero-description";

const Hero = () => {
  return (
    <section className="pt-16 xl:flex">
      <HeroDescription />
    </section>
  );
};

export default Hero;
